# ENGCE301 – Week 3  
Agile Requirements & Use Case Modeling + HTML/CSS Layout

Week 3 นี้เราจะทำ “ชุด Lab เดียวกัน” แต่แบ่งเป็น 2 ส่วนที่เชื่อมกัน:

1. **Requirements Lab** – SRS → User Stories → Use Case Diagram → Use Case Scenario  
2. **Web Lab (HTML/CSS)** – ออกแบบหน้าเว็บ Lab Submission Portal จาก Requirements ชุดเดียวกัน

ไฟล์ทั้งหมดในโฟลเดอร์นี้ออกแบบมาให้ใช้ร่วมกันในสัปดาห์นี้ครับ ✅

---

## 1. โครงไฟล์ในโฟลเดอร์นี้

```text
./
├─ README.md                (ไฟล์อธิบายงานทั้งหมดของ Week 3)
├─ ENGCE301_Week3_Teaching_Materials.pdf   (สไลด์ / เอกสารประกอบการสอน)
├─ LAB3_SRS.md              (SRS ตัวอย่างระบบ Lab Submission Portal)
├─ LAB3_UserStories.md      (User Stories + แนวทางใช้ใน LAB)
├─ LAB3_UseCaseExamples.md  (Template + ตัวอย่าง Use Case Scenario)
├─ index.html               (Landing Page – Lab Submission Portal)
├─ my-labs.html             (หน้า My Lab Status – mock data)
├─ about.html               (หน้าอธิบายระบบ + mapping SRS → UI)
└─ styles.css               (CSS กลาง ใช้ร่วมทุกหน้า)
```

> นักศึกษาควรอ่าน `ENGCE301_Week3_Teaching_Materials.pdf` คู่กับ README นี้
> เพื่อเข้าใจภาพรวมของวัตถุประสงค์การสอนและกิจกรรมในห้อง

---

## 2. Requirements Lab

(SRS → User Stories → Use Cases → Use Case Scenario)

### 2.1 อ่าน / ทำความเข้าใจ SRS (LAB3_SRS.md)

ไฟล์: `LAB3_SRS.md`
เนื้อหา: SRS ฉบับย่อของระบบ **ENGCE301 Lab Submission Portal** (ระบบส่งงาน LAB ผ่านเว็บ)

**สิ่งที่ต้องทำ**

* อ่าน SRS ให้เข้าใจ Actors, Functional Requirements (FR-1 … FR-8) และ NFR ต่าง ๆ
* กลุ่มของนักศึกษาสามารถ:

  * ใช้ SRS นี้ตรง ๆ เป็นระบบตัวอย่างของกลุ่ม
  * หรือ ปรับ/ขยายให้ใกล้เคียงกับ Term Project ของกลุ่ม (ถ้าอาจารย์อนุญาต)

> ผลลัพธ์: กลุ่มเข้าใจ “ภาพรวมระบบ” ที่จะใช้เป็นฐานสำหรับ User Stories + Use Cases

---

### 2.2 เขียน / ตรวจ User Stories (LAB3_UserStories.md)

ไฟล์: `LAB3_UserStories.md`
เนื้อหา: ชุด User Stories ของระบบ Lab Submission Portal + คำอธิบายวิธีใช้ใน LAB

**สิ่งที่ต้องทำ**

1. อ่าน User Stories ทั้งหมด (US-01 … US-08)
2. ตรวจว่าแต่ละ Story:

   * มีโครง As a / I want / So that ครบถ้วน
   * สอดคล้องกับ SRS (Actors + Functional Requirements)
3. ปรับ/เพิ่ม User Stories ตามระบบของกลุ่ม (ถ้าจำเป็น)
4. นำ User Stories ไปขึ้น **Product Backlog** บน Trello/Jira (ถ้าอาจารย์กำหนด)

> ผลลัพธ์ที่คาดหวัง
>
> * Product Backlog ที่มี User Stories อย่างน้อย 10–15 เรื่อง
> * แต่ละ Story มี Acceptance Criteria คร่าว ๆ และจัดลำดับความสำคัญแล้ว

---

### 2.3 ใช้ Use Case Template + ตัวอย่าง (LAB3_UseCaseExamples.md)

ไฟล์: `LAB3_UseCaseExamples.md`
เนื้อหา:

* Template สั้น ๆ สำหรับเขียน Use Case Scenario
* ตัวอย่าง Use Case Scenario ที่เขียนครบ (UC-01, UC-03, UC-04)

**สิ่งที่ต้องทำ**

1. จาก SRS + User Stories ให้กลุ่มของคุณเลือก Use Cases หลักอย่างน้อย 3 ตัว
   เช่น:

   * UC-01 View Lab List
   * UC-03 Submit Lab
   * UC-04 View My Lab Status
   * หรือ Use Case อื่น ๆ ที่กลุ่มออกแบบเอง
2. วาด **Use Case Diagram** ของระบบ Lab Submission Portal

   * ใช้ diagrams.net (draw.io) ตามที่สอนในคาบ
   * แสดง Actors (Student, Instructor, TA, …) และ Use Cases หลัก
   * ใส่ System Boundary และ Use Case Relations (include / extend) ถ้ามี
3. เขียน **Use Case Scenario** สำหรับ Use Cases ที่เลือก

   * คัดลอก Template จากไฟล์นี้
   * ระบุ: Preconditions, Postconditions, Main Flow, Alternative Flows
   * อย่างน้อย 2–3 Use Cases ต้องเขียน Scenario ให้ครบ

> ผลลัพธ์ที่คาดหวัง
>
> * 1 ไฟล์ Use Case Diagram (เช่น PNG หรือ PDF)
> * 1 ไฟล์ Use Case Scenarios (เช่น `.md`, `.docx` หรือ `.pdf`) ที่เขียนครบตาม Template

---

## 3. Web Lab (HTML/CSS) – Lab Submission Portal UI

เป้าหมายของส่วนนี้คือ นำสิ่งที่ทำใน Requirements Lab
(SRS, User Stories, Use Cases) มาใช้ในการออกแบบ **หน้าเว็บจริง** (Prototype UI)

มีหน้าเว็บหลัก 3 หน้า:

* `index.html` – Landing Page + Upcoming Labs + My Status (หน้าแรก)
* `my-labs.html` – หน้า My Lab Status (ขยายจากส่วนบน index)
* `about.html` – อธิบายระบบ และ mapping ระหว่าง Requirements → UI
* `styles.css` – CSS กลางที่ออกแบบมาก่อนแล้ว ~50%

### 3.1 สิ่งที่เตรียมให้แล้ว (~50%)

จากโค้ดเริ่มต้น:

* Layout หลัก: header, navbar, page container, sections, cards, table
* Hero section + Infographic เล็ก ๆ (SRS → User Stories → Use Cases → Web UI)
* ตัวอย่างการ์ด “LAB 3 – Agile Requirements & Use Case Modeling”
* ตารางสถานะ LAB ตัวอย่าง (LAB 1–3)
* Base styles ใน `styles.css`:

  * Design tokens (สี, shadow, radius)
  * Box Model, card styles, status badges
  * Responsive สำหรับจอเล็ก

นักศึกษาไม่ต้องเริ่มจากศูนย์ แต่ต้อง **อ่าน–เข้าใจ–ปรับให้เข้ากับ Requirements** ของกลุ่ม

---

### 3.2 งานที่นักศึกษาต้องทำใน Web Lab (~50%)

ให้ดู `TODO:` ที่กระจายอยู่ใน `index.html`, `about.html`, `my-labs.html`, `styles.css` แล้วทำสิ่งต่อไปนี้:

#### a) ปรับ `index.html`

* แก้ข้อความใน Hero section ให้ตรงกับระบบของกลุ่ม
  (อ้างอิง SRS + User Stories ที่เขียนไว้)
* เพิ่มการ์ดในส่วน **Upcoming Labs** อย่างน้อย 2 ใบ

  * ชื่อ LAB, คำอธิบายสั้น ๆ, deadline, tag (#UserStory #HTML #CSS ฯลฯ)
  * ให้สอดคล้องกับ Product Backlog ของกลุ่ม
* ปรับส่วน **My Lab Status (Mock Data)** ให้ตรงกับ LAB 1–3 (หรือมากกว่า) ของวิชา
  และใช้ class `status-done`, `status-pending`, `status-late` ตาม Late Submission Policy
  ที่กลุ่มกำหนดไว้ใน SRS / Use Case

#### b) ปรับ `about.html`

* เขียนคำอธิบาย System Overview ของ Lab Submission Portal
  โดยใช้ภาษาใน SRS ของกลุ่ม
* เขียน **mapping สั้น ๆ** ระหว่าง

  * SRS / Functional Requirements
  * User Stories
  * Use Cases
  * ส่วนต่าง ๆ ของ UI (เช่น Hero, Upcoming Labs, My Labs)

#### c) ปรับ `my-labs.html`

* ปรับตาราง/การ์ดสถานะให้สะท้อน LAB จริงของกลุ่ม
* สามารถเพิ่มคอลัมน์/ข้อมูล เช่น “Feedback สั้น ๆ”, “Last Updated” ฯลฯ ได้
* เชื่อมคำอธิบายด้านล่างกับ Policy จาก Use Case UC-03 / UC-04 ของกลุ่ม

#### d) ปรับ `styles.css`

* เลือก **Color Palette** ของกลุ่ม (ปรับตัวแปรใน `:root`)
* เลือก **Font** (เพิ่ม `<link>` Google Fonts ใน `.html` และเซ็ต `--font-sans`)
* ปรับ **margin / padding / border / hover effects** ให้หน้าเว็บอ่านง่าย สวยงาม
  และแสดง structure ของข้อมูลชัดเจน

---

## 4. สิ่งที่ต้องส่ง & วิธีการส่ง (ผ่าน LMS)

ภายใน **1 สัปดาห์หลังจากคาบ Week 3** (ก่อนเริ่มเรียน Week 4) ให้ส่งผ่านระบบ LMS ตามรายการนี้:

### 4.1 ส่วน Requirements
* ใช้ repo เดิมของกลุ่มได้ แต่สร้างชื่อ directory `lab3` เพื่อนำงานทั้งหมดใส่ในนั้น
* สร้าง `READEME.md` ของ lab3 ของกลุ่ม
  
1. **ลิงก์ Product Backlog** (Miro หรือ Trello / Jira)

   * ต้องมองเห็น User Stories ของระบบ Lab Submission Portal (หรือระบบของกลุ่ม)
   * ตั้งค่า Public / Share กับอาจารย์ให้เข้าดูได้
2. **Use Case Diagram**

   * รูปภาพ (.PNG/.JPG) หรือไฟล์ PDF จาก diagrams.net
   * แสดง Actors + Use Cases ครบตามที่กำหนด
3. **Use Case Scenarios**

   * ไฟล์ `.md` / `.docx` / `.pdf` ที่เขียนตาม Template ใน `LAB3_UseCaseExamples.md`
   * อย่างน้อย 2–3 Use Cases ต้องมี Main Flow + Alternative Flows

### 4.2 ส่วน Web Lab (HTML/CSS)
  * ส่งใน github.io หรือ [vercel](https://vercel.com/) ได้

1. **ลิงก์ Git Repository** ของกลุ่ม (GitHub / GitLab ฯลฯ)

   * ต้องมีโฟลเดอร์ที่มีไฟล์: `index.html`, `about.html`, `my-labs.html`, `styles.css`, `README.md`
   * ต้องมี commit ใหม่จากสัปดาห์นี้ (Week 3) แสดงการแก้ไข TODO และปรับ UI ตาม Requirements
2. (ถ้า LMS รองรับ) อาจแนบ **zip โฟลเดอร์เว็บ** เพิ่มเติม เพื่อสำรองการเปิดไฟล์ออฟไลน์

> **หมายเหตุ**
>
> * ใช้ repo เดิมของกลุ่มได้ แต่สร้างชื่อ directory `lab3` เพื่อนำงานทั้งหมดใส่ในนั้น
> * ตรวจให้แน่ใจว่าเปิดหน้า `index.html` จาก browser แล้ว layout แสดงผลได้ถูกต้อง

---

## 5. Checklist สำหรับนักศึกษา

ก่อนส่งงาน ตรวจสอบว่า:

* [ ] อ่านและเข้าใจ `LAB3_SRS.md` แล้ว
* [ ] ปรับ/ตรวจ `LAB3_UserStories.md` ให้สอดคล้องกับ SRS
* [ ] วาด Use Case Diagram และบันทึกเป็น PNG/PDF แล้ว
* [ ] เขียน Use Case Scenarios ในไฟล์เดียว (ตาม Template) อย่างน้อย 2–3 UC
* [ ] ปรับ `index.html`, `about.html`, `my-labs.html` ตาม TODO ครบถ้วน
* [ ] ปรับ `styles.css` ให้มี Color Palette + Typography ที่เลือกเอง
* [ ] Push โค้ดทั้งหมดขึ้น Git repository
* [ ] ส่งลิงก์ Miro, Trello/Jira, Use Case Diagram, Use Case Scenarios, และ Git repo ผ่าน LMS ครบ

เมื่อทำครบตาม README นี้ นักศึกษาจะได้ฝึก **ทั้ง Requirements Engineering และ Front-end Web**
ในบริบทของระบบเดียวกัน พร้อมสำหรับเนื้อหา Week 4 ต่อไปครับ 🚀

```
```